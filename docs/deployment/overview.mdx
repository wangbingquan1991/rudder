---
title: "Deployment"
description: "Rudder runtime modes and the documentation site deployment path."
---

Rudder supports local-first operation and authenticated deployments.

![Instance settings](../images/instance-settings.png)

<Note>
  This page describes Rudder product deployment modes. The separate "Documentation site deployment" section is only for publishing this Mintlify documentation site and is not required to use Rudder.
</Note>

## Runtime modes

| Mode | Exposure | Human auth | Primary use |
| --- | --- | --- | --- |
| `local_trusted` | n/a | No login required | Single-operator local machine workflow |
| `authenticated` | `private` | Login required | Private-network access such as Tailscale, VPN, or LAN |
| `authenticated` | `public` | Login required | Internet-facing or cloud-hosted deployment |

## Local trusted

`local_trusted` is optimized for a fast local setup. It binds to loopback and avoids login friction.

## Authenticated private

Authenticated private deployments require login while allowing lower-friction private-network URL handling.

## Authenticated public

Authenticated public deployments require login and stricter public URL checks. Use this mode for internet-facing installations.

## Documentation site deployment

This documentation site is a Mintlify project under `docs/`.

Local development:

```bash
pnpm docs:dev
```

Validation:

```bash
pnpm docs:validate
```

Deployment should be wired through Mintlify's GitHub integration for the `docs/` subdirectory, then pointed at `doc.rudder.zeeland.studio` as the production custom domain.

<Warning>
  Documentation site publishing is separate from Rudder runtime installation, CLI setup, and product deployment. The repository does not currently contain Vercel or Mintlify project metadata for `doc.rudder.zeeland.studio`; domain binding requires access to the Mintlify workspace and DNS provider.
</Warning>

## CLI relationship

The Rudder CLI starts and operates Rudder instances, sets local context, and manages control-plane objects such as issues and agents. It does not deploy the Mintlify documentation site. Use the CLI when running Rudder; use Mintlify's deployment flow when publishing these docs.

<CardGroup cols={2}>
  <Card title="Install Rudder" icon="download" href="/get-started/installation">
    Start Rudder Desktop and prepare the persistent CLI.
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/cli/reference">
    Use Rudder's command surface for instance and issue operations.
  </Card>
</CardGroup>
